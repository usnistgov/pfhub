---
# Luanch a workflow called surge to run surge-push on master. Required
# as jobs launched via pull-requests do not have access to secrets.
name: surge
on: [push, pull-request]
jobs:
  surge:
    runs-on: ubuntu-latest
    steps:
      - run: echo "prompt to launch surge"
