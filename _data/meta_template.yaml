---
benchmark:
  id: ${BENCHMARK_ID}
  version: ${{ env.benchmark_version }}
data: []
metadata:
  author:
    email:
    first:
    github_id: ${GITHUB_ID}
    last:
  hardware:
    acc_architecture:
    clock_rate:
    cores:
    cpu_architecture:
    nodes:
    parallel_model:
  implementation:
    container_url: ${CONTAINER}
    name: ${CODE_NAME}
    repo:
      url: ${REPO_URL}
      version: ${GIT_HASH}
  summary: ${SUMMARY}
  timestamp:
