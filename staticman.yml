# Name of the property. You can have multiple properties with completely
# different config blocks for different sections of your site.
# For example, you can have one property to handle comment submission and
# another one to handle posts.
simulations:
  allowedFields: ["benchmark", "metadata", "data"]
  allowedOrigins: ["localhost", "pages.nist.gov"]
  branch: "master"
  commitMessage: >-
    New simulation: {options.simulation_name}

    test the commit message 1

    test the commit message 2
  filename: "meta"
  extension: "yaml"
  format: "yaml"
  moderation: true
  name: "https://pages.nist.gov/chimad-phase-field/"
  path: "_data/simulations/{options.simulation_name}"
  requiredFields: ["benchmark", "metadata", "data"]
  generatedFields:
    date:
      type: date
      options:
        format: "timestamp-seconds"
