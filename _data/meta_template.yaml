---
benchmark:
  id: ${BENCHMARK_ID}
  version: 1
data: []
metadata:
  author:
    email:
    first:
    github_id: ${GITHUB_ID}
    last:
  hardware:
    acc_architecture:
    clock_rate:
    cores:
    cpu_architecture:
    nodes:
    parallel_model:
  implementation:
    container_url:
    name: ${CODE_NAME}
    repo:
      url: ${REPO_URL}
      version:
  summary: ${SUMMARY}
  timestamp:
