---
benchmark:
  id: ${BENCHMARK_ID}
  version: ${BENCHMARK_VERSION}
data: []
metadata:
  author:
    email: ${EMAIL}
    first:
    github_id: ${GITHUB_ID}
    last:
    name: ${AUTHOR_NAME}
  hardware:
    acc_architecture:
    clock_rate:
    cores:
    cpu_architecture:
    nodes:
    parallel_model:
  implementation:
    container_url: ${CONTAINER}
    name: ${CODE_NAME}
    repo:
      url: ${REPO_URL}
      version: ${GIT_HASH}
  summary: ${SUMMARY}
  timestamp: ${TIMESTAMP}
